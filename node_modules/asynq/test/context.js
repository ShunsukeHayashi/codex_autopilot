
var assert = require('assert');

var asynq = require('..');

var ctx = {
  some: 'thing'
};

describe('asynq.call(this)', function () {
  it('should pass the asynqntext', function () {
    return asynq.call(ctx, function *(){
      assert(ctx == this);
    });
  })
})
